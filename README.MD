# 🐍 Snake Game (Pygame + Jenkins + Docker)

A simple snake game built with **Pygame**, containerized using **Docker**, and tested via **Jenkins CI/CD**.

## Run locally
```bash
pip install -r requirements.txt
python app/main.py
# 🐍 Snake Game - Danush Grokr Edition

A fun Pygame-based snake game containerized with Docker and integrated into CI/CD using Jenkins and GitHub Actions.

### 🚀 Run locally
```bash
pip install -r requirements.txt
python app/main.py
